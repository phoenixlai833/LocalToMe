import ProfileSection from "./index.jsx";

export default {
   title: 'Organisms/ProfileSection',
   component: ProfileSection,
};

const Template = (args) => <ProfileSection {...args} />;

export const Default = Template.bind({});

Default.args = {
   
};